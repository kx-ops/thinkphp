

清理并重新构建
cd /d D:\github\hugo\kxops.com
rd /s /q public
rd /s /q resources
del /f /q .hugo_build.lock

本地预览测试
cd /d D:\github\hugo\kxops.com
hugo server -D

开始发布
hugo --minify --gc --cleanDestinationDir --baseURL "https://kxops.com/"

发布文章流程
hugo new posts/我的第一篇文章.md


# 添加所有变更
git add .
# 提交
git commit -m "Add first post: 我的文章"
git push origin main

每次修改
cd /d D:\github\hugo\kxops.com
rd /s /q public resources .hugo_build.lock
hugo --minify --gc --cleanDestinationDir --baseURL "https://kxops.com/"
git add .
git commit -m "argocd update"
git push origin main